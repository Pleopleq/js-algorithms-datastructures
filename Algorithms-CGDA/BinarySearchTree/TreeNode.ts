class TreeNode {
  value: number;
  leftChild: null | number;
  rightChild: null | number;

  constructor(value, leftNode, rightNode) {
    this.value = value;
    this.leftChild = leftNode;
    this.rightChild = rightNode;
  }

  insert(value, node) {
    
  }
}
